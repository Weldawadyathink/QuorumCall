QuorumCall is an app to manage groups of windows. A user adds a number of windows to a quorum. Then they can select the quorum to surface all windows in that quorum.

Assume that the user has this project open in xcode. If you need to edit a file that xcode will overwrite or block, use the question asking tool to ask the user to close xcode before you edit that file.
